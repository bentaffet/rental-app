# Neighborhood Map Fallbacks

These cached PNG map images are generated by
`frontend/scripts/generateNeighborhoodMaps.js` from OpenStreetMap tiles and a
local marker overlay. They are used when a listing image is missing or fails to
load.

Map data copyright OpenStreetMap contributors.
